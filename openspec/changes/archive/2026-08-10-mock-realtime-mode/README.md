
## 归档说明（2026-08-10）

实质全部完成（26/34 勾选）：Phase 1（Joi .when mock 模式 + AppModule 条件展开）、Phase 2
（mock-env 全链路，聚合层双源验证）、Step H（allowlist 内存解析 + 真实 sync 驱动，5.7 双源实测
全绿）。剩余 8 项为「## 4. 不做（明确边界）」节的刻意排除项（strikethrough，非 backlog）。
工具在 mist-datasource `tools/mock-env/`（run-mock.sh / mock-verify.sh，已切 OpenObserve 断言）。
