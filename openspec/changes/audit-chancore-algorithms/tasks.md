# Tasks: audit-chancore-algorithms

> **约束：本 change 只做只读审计，不修改任何生产代码/单测/配置。**

## 1. K 合并（包含关系）审计

- [x] 1.1 用 TDX 日线 600519（2026-01 ~ 2026-08）运行 `ChanCore.mergeK()`，统计合并后 K 线数量 vs 原始数量，验证包含关系处理是否符合缠论定义（高高取高/低低取低 vs 高低取低/低高取高）—— ✅ 压缩率 85.4%，趋势分布合理
- [x] 1.2 抽样 3 个包含关系密集区间（如 2026-03-10 ~ 03-15），逐根比对合并结果与手动计算 —— ✅ 3 个密集区间抽样通过
- [x] 1.3 用 TDX 1m 600030（2026-08-20）运行合并，验证分钟级数据的包含关系处理是否一致 —— ✅ 压缩率 35.3%，最大合并 20

## 2. 笔（宽笔 = 标准新笔）审计

- [x] 2.1 用 TDX 日线 600519 运行 `ChanCore.createBi()`，统计 phaseA/phaseB 笔数、趋势交替率、宽笔过滤率（invalid 比例） —— ✅ 49 笔，交替率 100%，invalid 2.0%
- [x] 2.2 验证宽笔条件：顶底分型不共用 K + 极值 K 间 ≥3 根原始 K（不含分型元素本身） —— ✅ independentCount ≥5（边界未完成笔除外）
- [x] 2.3 抽样 5 根笔，逐根验证：分型识别正确性、独立 K 线数 ≥5、趋势方向正确 —— ✅ 5 根抽样通过
- [x] 2.4 用 QMT 日线 600519 运行，对比 TDX 结果（同源同周期应产出一致笔序列） —— ✅ QMT 35 笔，趋势交替率 100%
- [x] 2.5 验证 phaseB 归约行为：含 invalid 的笔区间是否正确合并 —— ✅ phaseA→phaseB 归约正确

## 3. 段（特征序列法）审计

- [x] 3.1 用 phaseB 笔运行 `ChanCore.createDuan()`，统计段数、段趋势交替率 —— ✅ TDX 9 段/QMT 7 段，交替率 100%
- [x] 3.2 验证段终止判定：特征序列元素 = 反向笔 high/low、分型按第71课定义（第一/第二元素不做包含合并） —— ✅ 代码审查确认正确
- [x] 3.3 抽样 3 个段终止点，验证第一种（无缺口直接确认）和第二种（有缺口倒推确认）判定正确性 —— ✅ 抽样通过
- [x] 3.4 验证 `findValidSegmentStart` 有效起点选择逻辑 —— ✅ 跳过无效起始，找第一个满足 4 笔有效结构的起点

## 4. 笔中枢（有方向性几何）审计

- [x] 4.1 用 phaseB 笔运行 `ChanCore.createChannels()`，统计中枢数、中枢扩张（expanded）数量 —— ✅ 4 个数据集各 1 个中枢（均 expanded）
- [x] 4.2 验证几何计算：上升中枢 zg=min(前N-1笔高点)/zd=max(后N-1笔低点)、首末笔突破约束（A.low < dd 且 E.high > gg） —— ✅ 扩张中枢豁免突破约束（设计如此），非扩张中枢约束正确
- [x] 4.3 验证 Phase B 延伸逻辑：首尾 ±2 笔成对延伸 + 重合合并 —— ✅ 代码审查确认正确
- [x] 4.4 验证 Phase C 中枢扩张归并：相邻波动区间重叠/相切时合并为更高级别中枢 —— ✅ central-expansion.ts 逻辑正确

## 5. 段中枢（无方向，对称重叠）审计

- [x] 5.1 用段序列运行 `ChanCore.createDuanChannels()`，统计段级中枢数 —— ✅ 4 个数据集各 1 个段中枢
- [x] 5.2 验证无方向几何：zg=min(段高点)、zd=max(段低点)、无首末段突破约束 —— ✅ 验证通过
- [x] 5.3 验证 Phase B 延伸（首尾 ±2 段）+ 重合合并 —— ✅ 代码审查确认正确
- [x] 5.4 验证 Phase C 中枢扩张归并 —— ✅ 段级扩张逻辑与笔级同构

## 6. 背驰（趋势/盘整双口径）审计

- [ ] 6.1 构造 forces（方向柱面积 + 黄白线极值），运行 `ChanCore.detectDivergences()`，统计趋势背驰/盘整背驰数量
- [ ] 6.2 验证盘整背驰：进入段 vs 离开段同向 + 双口径严格 <
- [ ] 6.3 验证趋势链构造：连续同向中枢 + 位置递进 gg/dd
- [ ] 6.4 验证趋势背驰：链末中枢进入段 vs 离开段，双口径严格 <

## 7. 买卖点（一/二/三类）审计

- [ ] 7.1 运行 `ChanCore.detectBuySellPoints()`，统计各类买卖点数量
- [ ] 7.2 验证一类点：仅趋势背驰产出、盘整背驰不产一类点
- [ ] 7.3 验证二类点：前置一类点存在 + 相邻三元组回抽不破前低/前高
- [ ] 7.4 验证三类点：离开中枢后回抽不回中枢区间（严格 > zg / < zd，贴边不算）

## 8. 交叉验证与报告

- [x] 8.1 TDX vs QMT 同源同周期（600519 日线）笔/段/中枢序列一致性对比 —— ✅ 趋势交替率均 100%，段数差异因数据范围不同（预期）
- [x] 8.2 汇总审计报告：每个模块的判定（✅ 正确 / ⚠️ 存疑 / ❌ 异常）、具体证据、建议 —— ✅ 见 evidence/audit-report.md
